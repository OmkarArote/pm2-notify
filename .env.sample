# SMTP server configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# Set to 'true' for SSL (port 465), 'false' for STARTTLS (port 587). If not set, auto-detects based on port.
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Email addresses
MAIL_FROM="Your Name <from@example.com>"
MAIL_TO=to@example.com

# PM2 app names to monitor (comma-separated)
PM2_APPS=app1,app2

# How often to send emails (in milliseconds)
SEND_INTERVAL=10000